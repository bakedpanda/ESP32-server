---
phase: 07-setup-onboarding
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - README.md
autonomous: true
requirements:
  - SETUP-03
must_haves:
  truths:
    - "README MCP Tools table lists all 15 current tools including the 4 added in phases 5-6"
    - "README Architecture diagram tools/ section lists all 12 current modules including the 5 added in phases 5-6"
    - "README Setup section references setup.sh as the primary path with manual steps as fallback"
    - "README MCP registration command is accurate (--transport http, port 8000, /mcp path) with a note about hostname substitution"
    - "README Status table includes phases 4-6 (Hardening, Board Status, Provisioning)"
  artifacts:
    - path: "README.md"
      provides: "Accurate project documentation for v1.1"
      contains: "deploy_boot_config"
    - path: "README.md"
      provides: "setup.sh reference for new users"
      contains: "setup.sh"
  key_links:
    - from: "README.md MCP Tools table"
      to: "tests/test_readme.py parametrized tool list"
      via: "test_tool_in_readme assertions"
      pattern: "deploy_boot_config|get_board_status|check_board_health|discover_boards"
    - from: "README.md Architecture tools/ list"
      to: "tests/test_readme.py parametrized module list"
      via: "test_module_in_readme_architecture assertions"
      pattern: "board_status\\.py|credentials\\.py|boot_deploy\\.py|mdns_discovery\\.py|webrepl_cmd\\.py"
---

<objective>
Update README.md to accurately reflect the current state of the project: 15 MCP tools, 12 tool modules, setup.sh as the primary onboarding path, and accurate MCP registration instructions. Covers SETUP-03.

Purpose: A new user reading README.md sees the current tool set, knows to run setup.sh, and can register the server in Claude Code in one copy-paste command.
Output: Updated README.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-setup-onboarding/07-CONTEXT.md
@.planning/phases/07-setup-onboarding/07-RESEARCH.md
@.planning/phases/07-setup-onboarding/07-01-SUMMARY.md

<interfaces>
Current README state (read README.md before editing):
- MCP Tools table: 11 tools (missing: get_board_status, check_board_health, discover_boards, deploy_boot_config)
- Architecture tools/ list: 7 modules (missing: board_status.py, webrepl_cmd.py, mdns_discovery.py, credentials.py, boot_deploy.py)
- Setup: Steps 1-4 manual only (no reference to setup.sh)
- Status table: Phases 1-3 only (missing phases 4-6)

From tests/test_mcp_server.py — authoritative tool list (all 15):
list_connected_boards, identify_chip, flash_micropython, get_board_state,
deploy_file_to_board, deploy_directory_to_board, exec_repl_command,
read_board_serial, reset_board, deploy_ota_wifi, pull_and_deploy_github,
get_board_status, check_board_health, discover_boards, deploy_boot_config
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md — tools table, architecture, setup, status, MCP registration</name>
  <files>README.md</files>
  <read_first>
    - README.md (read the ENTIRE file first — executor must see the current structure to update it correctly, not guess)
    - tests/test_readme.py (read all parametrized tool and module names — these are the exact strings that must appear in the updated README)
    - tests/test_mcp_server.py (the expected list at lines 37-54 is authoritative for tool names and order)
    - .planning/phases/07-setup-onboarding/07-RESEARCH.md (the README Delta section at line 300+ specifies exactly which sections to change and what the target state is)
  </read_first>
  <action>
Update README.md with the following specific changes. Read the current file first to preserve existing content and structure. Make surgical edits — do not rewrite sections that are correct.

**Change 1: MCP Tools table — add 4 missing tools**

The current table ends with `pull_and_deploy_github`. Add these 4 rows after it (in this order, matching the registration order in mcp_server.py):

```markdown
| `get_board_status` | Query board firmware version, WiFi status, IP address, free memory, and free storage |
| `check_board_health` | Check whether MicroPython is running and the board is responsive; reports issues clearly |
| `discover_boards` | Discover MicroPython boards on the local network via mDNS; returns IP addresses |
| `deploy_boot_config` | Deploy a boot.py with WiFi, WebREPL, and mDNS advertisement config to a board, reading credentials from the Pi-local credentials file |
```

After this change, the table must have 15 rows.

**Change 2: Architecture diagram — add 5 missing tools/ modules**

The current architecture block ends with `└── github_deploy.py`. Replace the entire tools/ block with:

```
    └── tools/
        ├── board_detection.py  — USB enumeration, chip ID (esptool)
        ├── firmware_flash.py   — Firmware cache + flash (esptool)
        ├── file_deploy.py      — File/dir deploy (mpremote)
        ├── repl.py             — REPL exec, serial read, reset (mpremote)
        ├── serial_lock.py      — Per-port file lock (prevents USB conflicts)
        ├── ota_wifi.py         — WiFi OTA deploy (webrepl_cli.py subprocess)
        ├── github_deploy.py    — GitHub clone + deploy (git + deploy_directory)
        ├── board_status.py     — Board status queries (firmware, WiFi, memory, storage)
        ├── webrepl_cmd.py      — WebREPL command transport (over-WiFi REPL)
        ├── mdns_discovery.py   — mDNS board discovery via python-zeroconf
        ├── credentials.py      — WiFi + WebREPL credential loading from Pi-local file
        └── boot_deploy.py      — boot.py template generation and deployment
```

**Change 3: Setup section — add setup.sh as primary path**

Replace the existing Setup section (Steps 1-4) with:

```markdown
## Setup

**Quick setup (recommended)**

Run the setup script on your Pi. It handles everything: clones the repo, creates a virtualenv, installs dependencies, adds you to the `dialout` group, prompts for WiFi credentials, and installs+starts the systemd service.

```bash
curl -fsSL https://raw.githubusercontent.com/bakedpanda/ESP32-server/main/setup.sh | bash
```

Or clone first and run locally:

```bash
git clone https://github.com/bakedpanda/ESP32-server.git esp32-station
bash esp32-station/setup.sh
```

The script prints the `claude mcp add` command at the end — copy-paste it on your main machine to register the server.

**Manual setup (fallback)**

If you prefer to do it step by step:

**1. Clone and install dependencies**

```bash
git clone https://github.com/bakedpanda/ESP32-server.git esp32-station
cd esp32-station
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

**2. Add serial port permissions**

```bash
sudo usermod -aG dialout $USER
# Log out and back in for this to take effect
```

**3. Create WiFi credentials file**

```bash
sudo mkdir -p /etc/esp32-station
sudo tee /etc/esp32-station/wifi.json << 'EOF'
{"ssid": "YOUR_SSID", "password": "YOUR_WIFI_PASSWORD", "webrepl_password": "YOUR_WEBREPL_PASS"}
EOF
sudo chmod 600 /etc/esp32-station/wifi.json
```

**4. Install the systemd service**

Edit `esp32-station.service` to replace `User=esp32` and `/home/esp32` with your actual username and home directory, then:

```bash
sudo cp esp32-station.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable esp32-station
sudo systemctl start esp32-station
```

**5. Register with Claude Code (on your main machine)**

```bash
claude mcp add --transport http esp32-station http://raspberrypi.local:8000/mcp
```

Replace `raspberrypi.local` with your Pi's actual hostname or IP address. Find the hostname with `hostname` and the IP with `hostname -I`.
```

**Change 4: Status table — add phases 4-6**

Find the Status table (currently shows Phase 1-3) and add rows for phases 4-6 after Phase 3:

```markdown
| 4 — Hardening | Reliable resets (DTR/RTS), explicit --chip enforcement, tech debt cleanup | Complete |
| 5 — Board Status | Board health check, firmware query, WiFi status, mDNS discovery | Complete |
| 6 — Provisioning | Always-erase flash, WiFi credential management, boot.py deployment | Complete |
| 7 — Setup & Onboarding | One-command Pi setup script, updated documentation | Complete |
```

Note: Phase 7 row is added as "Complete" since this is the final task of Phase 7. If the Status table header row currently says "Phase | What it delivers | Status", keep that header — just add the new rows.

**No other changes.** Preserve all other README content exactly as-is. Do not change the project description, requirements section, running tests section, or out-of-scope section.
  </action>
  <verify>
    <automated>cd /mnt/anton/Claude/ESP32-server && source venv/bin/activate && pytest tests/test_readme.py -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "deploy_boot_config" README.md outputs a match
    - grep "get_board_status" README.md outputs a match
    - grep "check_board_health" README.md outputs a match
    - grep "discover_boards" README.md outputs a match
    - grep "board_status.py" README.md outputs a match
    - grep "webrepl_cmd.py" README.md outputs a match
    - grep "mdns_discovery.py" README.md outputs a match
    - grep "credentials.py" README.md outputs a match
    - grep "boot_deploy.py" README.md outputs a match
    - grep "setup.sh" README.md outputs a match (minimum 2 occurrences — quick setup and manual section reference)
    - grep "claude mcp add --transport http esp32-station" README.md outputs a match
    - grep "hostname" README.md and grep "IP" README.md both output matches (hostname substitution note)
    - grep -c "| \`" README.md returns 15 or more (15 rows in MCP Tools table, each row starts with "| `")
    - grep "Phase 4\|Hardening" README.md outputs a match
    - grep "Phase 5\|Board Status" README.md outputs a match
    - grep "Phase 6\|Provisioning" README.md outputs a match
    - pytest tests/test_readme.py -v exits 0 with all tests passing
  </acceptance_criteria>
  <done>All tests in tests/test_readme.py pass. README.md contains all 15 tool names, all 12 architecture modules, setup.sh reference, accurate MCP registration command, and phases 4-6 in status table.</done>
</task>

</tasks>

<verification>
```bash
cd /mnt/anton/Claude/ESP32-server && source venv/bin/activate && pytest tests/test_readme.py -v 2>&1 | tail -30
```

All parametrized tool and module tests must pass. No failures.
</verification>

<success_criteria>
- README.md MCP Tools table has 15 rows (all tools including get_board_status, check_board_health, discover_boards, deploy_boot_config)
- README.md Architecture section lists all 12 tools/ modules including board_status.py, webrepl_cmd.py, mdns_discovery.py, credentials.py, boot_deploy.py
- README.md Setup section references setup.sh as primary path with manual steps as fallback
- README.md contains the MCP registration command with --transport http and hostname substitution note
- README.md Status table includes phases 4-7
- All tests in tests/test_readme.py pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-setup-onboarding/07-03-SUMMARY.md`
</output>
